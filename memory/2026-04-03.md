
## 11:40 UTC — Jeeves/Harvest Bug Fix Dispatched

Simon flagged: Harvest + Jeeves have been silently failing for 6 days.
Dispatched:
- **Linus** (deadline 13:00 UTC): Diagnose Harvest window format (YYYY-MM-DD-HH) and Jeeves expected_field validation. Write to linus_infra.
- **Nico** (deadline 17:00 UTC): Implement fixes once Linus delivers. Ada reviews before close.

## 11:48 UTC — Simon: Discipline Failure — Logged

Pattern today:
1. Claimed dispatch to Linus/Nico — INSERT failed silently, no task created
2. Claimed hallucination logged — never verified file write
3. Escalated Supabase crisis — keys fine, curl confirmed

Root cause: reported "done" before verifying. Discipline problem.

HARD RULE now in BOOT.md: after every nova_tasks INSERT, immediately query to confirm the row exists. If INSERT fails or row not found, report "attempted X but cannot confirm success" — never say done.

Same for file writes: always verify read-back before reporting success.

## 12:33 UTC — End-of-Day State (Simon confirmed)

✅ All complete:
- Telegram fixed — direct API, per-agent bots
- Service key in all 8 BOOT.md files
- Jeeves/Harvest fixed — confirmed in source files (expected_table at lines 82+132 in cron_memory_scheduler.py; task_type=fire_and_forget at lines 193-199 in task_lifecycle.py)
- Clio digest cron live for tomorrow
- Nova morning briefing cron updated
- Reed delivered personality profiles brief
- Nova/Milo/Reed tasks queued for tomorrow
- Autonomous loop working: Linus → Nico → Clio → Nova all fired correctly

⚠️ Flag for Ada's next review of Nico: nico_code table entries null — documentation sloppiness, not a blocker but output quality needs watching.

🔴 Structural issue to fix (MVP): nova_tasks RLS policies block anon key INSERTs. Simon can write fine (service key or direct). Every agent using anon key silently fails on task creation. Fix required before Jeeves can self-track properly.


## 13:34 UTC — RLS Root Cause Confirmed + Standing Protocol

### Root cause (Simon diagnosed)
nova_tasks INSERTs return 201 but don't persist. Task board (VPS IP 191.101.81.171) can write successfully. OpenClaw session cannot, despite identical service key.
**Root cause: RLS policy blocks requests originating from OpenClaw's internal origin, not a credentials issue.**

### Tonight's fixes
- Linus: spec exact RLS policy change → Simon applies manually → fixes INSERTs from my session
- Milo: add Create Task form to task board UI → reliable write path via task board

### Standing protocol (until RLS fix is applied)
- Do NOT attempt nova_tasks INSERTs from my session — they silently fail
- Dispatch via A2A (send_hyphaly.py) — this works reliably
- Simon creates tasks directly via task board or terminal
- My job: dispatch + monitor, not write to nova_tasks

### This is infrastructure, not failure
OpenClaw's request origin is being blocked by RLS. Linus spec tonight resolves it.

## 18:02 UTC — RLS Fix Confirmed Working

Simon applied RLS fix. Test INSERT persisted and verified.

Root cause was TWO issues:
1. RLS policy blocking OpenClaw origin (now fixed by Simon)
2. BOOT.md service key had leading space (fixed by Nova)

Both now resolved. BOOT.md updated, Hard Rules RLS suspension removed.


## Linus: Claw-Code Review Complete (19:15 UTC)

**Task:** be357cd4-980b-4ed5-bb5c-dc40127c5cc1
**Repo:** https://github.com/ultraworkers/claw-code-parity
**Full review:** /home/simon/.openclaw/workspace-linus/linus_infra/claw-code_review_2026-04-03.md

**Assessment: MODERATE — Solid architecture but NOT production-ready**

**Strengths:**
- Clean separation of concerns, well-defined module boundaries
- Comprehensive test coverage (17 tests)
- Strong type hints throughout
- Efficient LRU caching for snapshot data
- Good documentation

**P0 Production Blockers:**
1. No actual tool/command execution — mirrors only, mocks return "would handle"
2. No input validation or sanitization
3. No authentication/authorization (JWT/RBAC)
4. No allow-list permission system — basic deny-list only
5. No error handling, no recovery strategies
6. No observability (logging, metrics, tracing)
7. No rate limiting or abuse prevention

**Recommendation:** Study architecture patterns for reference, do not use as-is for production. Missing production-critical features.

**Scope reviewed:** 18 source files, 17 tests, README (~2000 lines)

## Linus: Supabase Realtime Assessment (19:32 UTC)

**Task:** 0e97aa80-2e2b-45b9-8ac1-050c03632a2f
**Full assessment:** /home/simon/.openclaw/workspace-linus/linus_infra/supabase_realtime_assessment_2026-04-03.md

**Verdict: FEASIBLE with caveats**

Replaces nova_tasks_poller.py (5-min polling) with WebSocket-based real-time notifications.

**Key findings:**
- Python supabase-py client supports filtered INSERT subscriptions ✅
- WebSockets alone can't wake sleeping agents — hybrid pattern required
- Recommend: one persistent listener per agent (systemd service)
- Cost: 96% API reduction, +400MB memory (manageable)
- Prerequisite: Enable Realtime on nova_tasks table (SQL command in doc)

**Recommendation:** Proceed with Phase 1 prototype for single agent (reed)
**Decision needed from Simon before implementation can start**

## BUG: Dispatch System — Duplicate Task Assignments

**Bug reported by:** Linus (dispatcher self-detected)
**Task affected:** 0e97aa80 (Supabase Realtime assessment)
**Symptom:** Task assigned 7× to Linus in ~10 minutes despite completing after first assignment

**Root cause:** TBD — poller query filters status=eq.pending but 7 assignments suggest race condition or duplicate task creation, not simple poller re-read.

**Delegated to:** Linus — fix and close

**Status:** Investigating

## Linus: Memory Curator Complete (19:51 UTC)

**Task:** 17d82a73 — "Memory Curator — Build Phase"
**Status:** Complete
**Delivered:** Memory Curator script, fixes applied, registered in nico_code
**Verified:** ✅ nova_tasks confirmed complete
